import React from 'react';
import Search from "./Search";

const Header = () => {

    return (
        <div className="container">
                <Search/>
        </div>
    );
};

export default Header;